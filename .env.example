# Copy this file to `.env` and fill in real values. Keep `.env` out of git.

# OpenAI (required for LLM calls)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Optional overrides for self-hosted / Azure style endpoints
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_ORG_ID=
# OPENAI_PROJECT_ID=

# Execution mode guard
# Set to 1 to allow real API calls; keep 0 for mock-only runs in CI or without keys.
ALLOW_REAL=0

# Slack (User Token starting with xoxp-; required for real Slack searches)
SLACK_USER_TOKEN=xoxp-your-slack-user-token

# GitHub (PAT with repo read scope; fine-grained or classic)
GITHUB_TOKEN=ghp_your_github_pat
# Repository to use for real smoke search (owner/repo)
GITHUB_SMOKE_REPO=your-org/your-repo
# Optional search query override (default: "smoke")
# GITHUB_SMOKE_QUERY=smoke

# Google Drive (paths to local credential files; never commit these)
# OAuth token saved after first consent; e.g., secrets/token.json
DRIVE_TOKEN_PATH=secrets/token.json
# Client credentials JSON downloaded from Google Cloud console
GOOGLE_CREDENTIALS_PATH=secrets/credentials.json
# Aliases for the Node gdrive server (@modelcontextprotocol/server-gdrive)
GDRIVE_CREDENTIALS_PATH=${DRIVE_TOKEN_PATH}
GDRIVE_OAUTH_PATH=${GOOGLE_CREDENTIALS_PATH}

# Optional smoke search queries
# SLACK_SMOKE_QUERY=smoke
# DRIVE_SMOKE_QUERY="name contains 'smoke'"
